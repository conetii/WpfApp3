<Window x:Class="WpfApp3.Dialogs.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Window.Resources>
        <Style x:Key="DialogButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource PanelAltBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource PopupHighlightBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Background" Value="{DynamicResource PanelPressedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryDialogButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource ControlFocusBackgroundBrush}"/>
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource PopupBackgroundBrush}"
            BorderBrush="{DynamicResource PopupBorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="Transparent"
                    Padding="0,0,0,10"
                    MouseLeftButtonDown="HeaderBorder_MouseLeftButtonDown">
                <TextBlock x:Name="TitleTextBlock"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource PopupHighlightTextBrush}"/>
            </Border>

            <TextBlock x:Name="PromptTextBlock"
                       Grid.Row="1"
                       Margin="0,0,0,12"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}"/>

            <TextBlock x:Name="PasswordLabelTextBlock"
                       Grid.Row="2"
                       Margin="0,0,0,6"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>

            <PasswordBox x:Name="PasswordValueBox"
                         Grid.Row="3"
                         Height="34"
                         Padding="10,6"
                         Background="{DynamicResource SurfaceBackgroundBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource PopupBorderBrush}"
                         BorderThickness="1"
                         PasswordChanged="PasswordBoxes_PasswordChanged"/>

            <TextBlock x:Name="ConfirmPasswordLabelTextBlock"
                       Grid.Row="4"
                       Margin="0,12,0,6"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>

            <PasswordBox x:Name="ConfirmPasswordValueBox"
                         Grid.Row="5"
                         Height="34"
                         Padding="10,6"
                         Background="{DynamicResource SurfaceBackgroundBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource PopupBorderBrush}"
                         BorderThickness="1"
                         PasswordChanged="PasswordBoxes_PasswordChanged"/>

            <TextBlock x:Name="ValidationTextBlock"
                       Grid.Row="6"
                       Margin="0,12,0,0"
                       Foreground="{DynamicResource StatusWarningBrush}"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"/>

            <StackPanel Grid.Row="7"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button x:Name="CancelButton"
                        Style="{StaticResource DialogButtonStyle}"
                        Click="CancelButton_Click"/>
                <Button x:Name="ActionButton"
                        Style="{StaticResource PrimaryDialogButtonStyle}"
                        Click="ActionButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>